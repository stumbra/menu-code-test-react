import styled from "styled-components";

const Wrapper = styled.div`
   display: flex;
   justify-content: center;
   padding-bottom: 20px;
`;

const SaveButtonWrapper = styled.div`
   margin: 0 15px;
   align-self: center;
   margin-top: 5px;
`;

export { Wrapper, SaveButtonWrapper };
